# Trade11

Trade11 is a Vite, React, TypeScript and Capacitor app for managing a sticker album and coordinating trades between collectors.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

## iOS

The iOS shell lives in `ios/App` and loads the web build from `dist` through Capacitor.
